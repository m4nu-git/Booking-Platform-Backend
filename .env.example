# Root .env file — used by docker-compose.yml for values that can't be hardcoded.
# Copy this file to .env and fill in your real values before running `make docker-up`.

# Gmail credentials for NotificationService email sending
MAIL_USER=your-email@gmail.com
MAIL_PASS=your-gmail-app-password
